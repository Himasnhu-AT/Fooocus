# Ensure that shell scripts always use lf line endings, e.g. entrypoint.sh for docker
* text=auto
*.sh text eol=lf
**/*.png filter=lfs diff=lfs merge=lfs -text
models/vae_approx/taesdxl_decoder.pth filter=lfs diff=lfs merge=lfs -text
